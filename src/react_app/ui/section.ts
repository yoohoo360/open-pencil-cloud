import { tv } from 'tailwind-variants'

export const sectionWrapper = tv({
  base: 'border-b border-border px-3 py-2'
})

export const sectionLabel = tv({
  base: 'mb-1 block text-[11px] text-muted'
})
